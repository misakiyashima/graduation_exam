<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>地図から探す</title>
  <style>
    #map {
      height: 500px;
      width: 100%;
    }
  </style>
</head>

<body>
  <h1>オールインクルーシブホテルを地図から探す</h1>
  <div id="map"></div>
<div id="map" data-google-maps-api-key="<%= ENV['GOOGLE_MAPS_API_KEY'] %>" data-hotels='<%= raw(@hotels.to_json) %>'></div>

<div id="map" style="height:500px;width:100%;"></div>

<script>
  // テスト用の簡易 initMap をページに直接定義（callback に必要）
  window.initMap = function() {
    const el = document.getElementById('map');
    if (!el) { console.error('map element not found'); return; }
    // 東京近辺表示（とりあえず地図が出るか確認）
    const map = new google.maps.Map(el, { center: { lat: 35.6895, lng: 139.6917 }, zoom: 10 });
  };
</script>

<script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['GOOGLE_MAPS_API_KEY'] %>&callback=initMap" async defer></script>

</body>

<div>
  <%= link_to 'ホームに戻る', root_path, class: 'btn btn-primary' %>
</div>
</html>
