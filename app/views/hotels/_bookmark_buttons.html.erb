<div class='bookmark-buttons-wrapper'>
  <% if current_user && current_user.bookmark?(hotel[:hotelNo]) %>
    <%= render 'unbookmark', { hotel: hotel } %>
  <% else %>
    <%= render 'bookmark', { hotel: hotel } %>
  <% end %>
</div>
