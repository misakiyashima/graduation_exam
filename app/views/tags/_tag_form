<%= form_with url: add_tag_path, method: :post do %>
  <div>
    <%= label_tag :hotel_id, "ホテル" %>
    <%= select_tag :hotel_id, options_from_collection_for_select(Hotel.all, :id, :name) %>
  </div>
  <div>
    <%= label_tag :tag_id, "タグ" %>
    <%= select_tag :tag_id, options_from_collection_for_select(Tag.all, :id, :name) %>
  </div>
  <div>
    <%= submit_tag "タグを追加" %>
  </div>
<% end %>
